name: Automatic external contribution publication disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic publication of external contributions is disabled; the owner-authorized Atlas updater uses its own fully checked PR flow."
